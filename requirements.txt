
fastapi==0.141.1
# Starlette is fastapi's transitive dependency, pinned explicitly here
# rather than left implicit: older Starlette versions (as fastapi==0.115.0
# was shipping) are vulnerable to CVE-2026-48710 ("BADHOST" - a malformed
# Host header can desync request.url.path from the real request path,
# potentially bypassing any path-based access check) and CVE-2025-62727
# (a CVSS 8.7 unauthenticated ReDoS in FileResponse's Range-header parsing,
# directly reachable via this app's own /download and /download-exec
# routes, which use FileResponse). 1.0.1+ fixes both.
starlette>=1.0.1
uvicorn[standard]==0.30.6
# Was 0.0.9 - vulnerable to CVE-2024-53981, an unauthenticated DoS via a
# crafted multipart request (reachable through every file-upload form on
# this site). 0.0.18+ fixes it.
python-multipart==0.0.18
anthropic==0.39.0
httpx==0.27.2
python-docx==1.1.2
pypdf==4.3.1
pymupdf==1.24.10
openpyxl==3.1.5
jinja2==3.1.4
playwright==1.47.0
